# Frontend (Next.js)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

By default the frontend calls the backend at `http://localhost:8000`. You can change this with the env var `NEXT_PUBLIC_API_URL`.
